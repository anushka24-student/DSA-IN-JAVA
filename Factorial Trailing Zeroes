
// Here factorial has not been found instead a simple trick is used that is (Trailing Zero's == Count of 5's)

class Solution {
    public int trailingZeroes(int n) {

        int count = 0;

        while(n > 0){
            n = n / 5;
            count = count + n;
        }

        return count;
    }
}
