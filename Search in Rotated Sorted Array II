class Solution {
    public boolean search(int[] nums, int target) {
        Arrays.sort(nums);
        int n=nums.length;
        boolean found = false;
        for(int i=0;i<n;i++){
            if(nums[i]==target){
                found=true;
                break;
            }
        }
        return found;
    }
}
